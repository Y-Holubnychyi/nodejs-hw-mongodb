description: Forbidden - you do not have access to this resource
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - message
      properties:
        status:
          type: integer
          example: 403
        message:
          type: string
          example: Access denied
